const TARGET_LIBRARY = {
  m81: {
    name: "Bode's Galaxy",
    catalog: "M81",
    category: "Deep Sky",
    objectType: "Grand Design Spiral Galaxy",
    constellation: "Ursa Major",
    distance: "~12 million light-years",
    apparentMagnitude: "6.9",
    diameter: "~90,000 light-years",
    discovered: "1774",
    shortDescription: "A bright spiral galaxy and one of the best galaxy targets for beginner astrophotography.",
    description: "M81, also known as Bode's Galaxy, is a large spiral galaxy in Ursa Major. It is one of the brightest galaxies visible from Earth and is often photographed with nearby M82.",
    whatYouAreSeeing: "The bright central core contains billions of older stars, while the faint outer disk and spiral arms contain younger stars, dust, and star-forming regions.",
    whyItIsInteresting: "M81 is gravitationally interacting with nearby M82. That interaction helps make this galaxy pair one of the most interesting wide-field galaxy targets for amateur telescopes.",
    captureNotes: "Captured with a Seestar S30."
  },

  m82: {
    name: "Cigar Galaxy",
    catalog: "M82",
    category: "Deep Sky",
    objectType: "Starburst Galaxy",
    constellation: "Ursa Major",
    distance: "~12 million light-years",
    apparentMagnitude: "8.4",
    diameter: "~37,000 light-years",
    discovered: "1774",
    shortDescription: "A thin, bright galaxy known for intense star formation.",
    description: "M82, the Cigar Galaxy, is a starburst galaxy near M81. It appears long and narrow because we see it nearly edge-on.",
    whatYouAreSeeing: "The bright cigar-shaped body is a galaxy undergoing intense star formation, likely triggered by its interaction with M81.",
    whyItIsInteresting: "M82 is one of the nearest and brightest starburst galaxies, making it a dramatic companion to M81 in astrophotography.",
    captureNotes: "Captured with a Seestar S30."
  },

  m51: {
    name: "Whirlpool Galaxy",
    catalog: "M51",
    category: "Deep Sky",
    objectType: "Interacting Spiral Galaxy",
    constellation: "Canes Venatici",
    distance: "~31 million light-years",
    apparentMagnitude: "8.4",
    diameter: "~76,000 light-years",
    discovered: "1773",
    shortDescription: "A famous spiral galaxy interacting with a smaller companion galaxy.",
    description: "M51, the Whirlpool Galaxy, is one of the most recognizable spiral galaxies in the night sky. Its spiral arms and companion galaxy make it a favorite astrophotography target.",
    whatYouAreSeeing: "The larger spiral galaxy is interacting with a smaller companion, which helps stretch and shape its spiral arms.",
    whyItIsInteresting: "M51 is a classic example of gravitational interaction between galaxies and is one of the best spring galaxy targets.",
    captureNotes: "Captured with a Seestar S30."
  },

  m104: {
    name: "Sombrero Galaxy",
    catalog: "M104",
    category: "Deep Sky",
    objectType: "Spiral Galaxy",
    constellation: "Virgo",
    distance: "~29 million light-years",
    apparentMagnitude: "8.0",
    diameter: "~50,000 light-years",
    discovered: "1781",
    shortDescription: "A galaxy with a bright core and dark dust lane that gives it a sombrero-like shape.",
    description: "M104, the Sombrero Galaxy, is famous for its bright central bulge and dark dust lane.",
    whatYouAreSeeing: "The bright central region is the galaxy's core, while the dark band across it is dust blocking some of the starlight.",
    whyItIsInteresting: "Its unusual edge-on appearance makes it one of the most visually distinctive galaxies for small telescopes.",
    captureNotes: "Captured with a Seestar S30."
  },

  m97: {
    name: "Owl Nebula",
    catalog: "M97",
    category: "Deep Sky",
    objectType: "Planetary Nebula",
    constellation: "Ursa Major",
    distance: "~2,000 light-years",
    apparentMagnitude: "9.9",
    diameter: "~2 light-years",
    discovered: "1781",
    shortDescription: "A faint planetary nebula with an owl-like face.",
    description: "M97, the Owl Nebula, is a planetary nebula formed when a dying star shed its outer layers into space.",
    whatYouAreSeeing: "The round glow is expanding gas from an old star. The darker areas give it the appearance of an owl's eyes.",
    whyItIsInteresting: "It shows the late-life stage of a Sun-like star and is a challenging but rewarding small-telescope target.",
    captureNotes: "Captured with a Seestar S30."
  },

  m13: {
    name: "Great Hercules Cluster",
    catalog: "M13",
    category: "Deep Sky",
    objectType: "Globular Cluster",
    constellation: "Hercules",
    distance: "~22,000 light-years",
    apparentMagnitude: "5.8",
    diameter: "~145 light-years",
    discovered: "1714",
    shortDescription: "One of the brightest globular clusters in the northern sky.",
    description: "M13 is a dense ball of hundreds of thousands of ancient stars orbiting the Milky Way.",
    whatYouAreSeeing: "The sparkling ball is a tightly packed cluster of old stars, many billions of years old.",
    whyItIsInteresting: "M13 is one of the best globular clusters for beginner astrophotography because it is bright and resolves into many stars.",
    captureNotes: "Captured with a Seestar S30."
  },

  m27: {
    name: "Dumbbell Nebula",
    catalog: "M27",
    category: "Deep Sky",
    objectType: "Planetary Nebula",
    constellation: "Vulpecula",
    distance: "~1,360 light-years",
    apparentMagnitude: "7.5",
    diameter: "~2.9 light-years",
    discovered: "1764",
    shortDescription: "A bright planetary nebula and excellent summer target.",
    description: "M27, the Dumbbell Nebula, is a planetary nebula created by a dying star shedding its outer layers.",
    whatYouAreSeeing: "The glowing gas was expelled from an aging star. The central star is heating the gas and causing it to glow.",
    whyItIsInteresting: "M27 is bright, colorful, and one of the easiest nebulae to capture with a small smart telescope.",
    captureNotes: "Captured with a Seestar S30."
  },

  m57: {
    name: "Ring Nebula",
    catalog: "M57",
    category: "Deep Sky",
    objectType: "Planetary Nebula",
    constellation: "Lyra",
    distance: "~2,500 light-years",
    apparentMagnitude: "8.8",
    diameter: "~1 light-year",
    discovered: "1779",
    shortDescription: "A small ring-shaped planetary nebula in Lyra.",
    description: "M57, the Ring Nebula, is a shell of gas from a dying star.",
    whatYouAreSeeing: "The ring is glowing gas expanding away from a central white dwarf star.",
    whyItIsInteresting: "It is one of the most famous planetary nebulae and shows the future fate of Sun-like stars.",
    captureNotes: "Captured with a Seestar S30."
  },

  m44: {
    name: "Beehive Cluster",
    catalog: "M44",
    category: "Deep Sky",
    objectType: "Open Star Cluster",
    constellation: "Cancer",
    distance: "~610 light-years",
    apparentMagnitude: "3.7",
    diameter: "~15 light-years",
    discovered: "Known since ancient times",
    shortDescription: "A bright open cluster of young stars.",
    description: "M44, the Beehive Cluster, is a nearby open star cluster visible to the naked eye under dark skies.",
    whatYouAreSeeing: "The stars in this cluster formed together from the same cloud of gas and dust.",
    whyItIsInteresting: "It is one of the closest and brightest open clusters, making it a great beginner target.",
    captureNotes: "Captured with a Seestar S30."
  },

  ngc4565: {
    name: "Needle Galaxy",
    catalog: "NGC 4565",
    category: "Deep Sky",
    objectType: "Edge-on Spiral Galaxy",
    constellation: "Coma Berenices",
    distance: "~40 million light-years",
    apparentMagnitude: "10.4",
    diameter: "~100,000 light-years",
    discovered: "1785",
    shortDescription: "A thin edge-on galaxy with a prominent dust lane.",
    description: "NGC 4565, the Needle Galaxy, is a spiral galaxy seen almost perfectly edge-on.",
    whatYouAreSeeing: "The thin line is the galaxy's disk viewed from the side. The darker central band is dust blocking some of the starlight.",
    whyItIsInteresting: "It gives a dramatic side-view of what a spiral galaxy disk can look like.",
    captureNotes: "Captured with a Seestar S30."
  },

  sun: {
    name: "Sun",
    catalog: "",
    category: "Solar System",
    objectType: "Star",
    constellation: "",
    distance: "~93 million miles",
    apparentMagnitude: "-26.7",
    diameter: "~864,000 miles",
    discovered: "Known since ancient times",
    shortDescription: "Our home star, seen in visible light.",
    description: "The Sun is the star at the center of our Solar System and the source of nearly all energy on Earth.",
    whatYouAreSeeing: "Depending on conditions, the image may show the solar disk and sunspots.",
    whyItIsInteresting: "Sunspots are cooler, magnetically active regions that change from day to day.",
    captureNotes: "Captured safely using the Seestar solar filter."
  },

  moon: {
    name: "Moon",
    catalog: "",
    category: "Solar System",
    objectType: "Natural Satellite",
    constellation: "",
    distance: "~239,000 miles",
    apparentMagnitude: "Varies",
    diameter: "~2,159 miles",
    discovered: "Known since ancient times",
    shortDescription: "Earth's natural satellite.",
    description: "The Moon is Earth's only natural satellite and one of the easiest and most rewarding targets to image.",
    whatYouAreSeeing: "The bright and dark regions are lunar highlands and maria, with craters visible along the terminator.",
    whyItIsInteresting: "The Moon changes appearance every night as sunlight strikes it from different angles.",
    captureNotes: "Captured with a Seestar S30."
  },

  sirius: {
    name: "Sirius",
    catalog: "",
    category: "Deep Sky",
    objectType: "Star",
    constellation: "Canis Major",
    distance: "~8.6 light-years",
    apparentMagnitude: "-1.46",
    diameter: "",
    discovered: "Known since ancient times",
    shortDescription: "The brightest star in Earth's night sky.",
    description: "Sirius is a nearby bright star in Canis Major and is actually part of a binary star system.",
    whatYouAreSeeing: "The bright point of light is Sirius A. Its faint white dwarf companion, Sirius B, is usually difficult to see in small telescope images.",
    whyItIsInteresting: "Sirius is one of the nearest star systems and the brightest star visible from Earth at night.",
    captureNotes: "Captured with a Seestar S30."
  }
};

function normalizeTargetName(value) {
  return String(value || "")
    .toLowerCase()
    .replace(/messier\s*/g, "m")
    .replace(/ngc\s*/g, "ngc")
    .replace(/[^a-z0-9]/g, "");
}

function getStarterInfoForFolder(folder) {
  const folderName = folder.split("/").pop() || "";
  const key = normalizeTargetName(folderName);
  const info = TARGET_LIBRARY[key];

  if (info) {
    return {
      ...info,
      dateCaptured: "",
      equipment: "Seestar S30",
      location: "Florida, USA"
    };
  }

  return {
    name: folderName,
    catalog: "",
    category: folder.startsWith("solar-system/") ? "Solar System" : "Deep Sky",
    objectType: "",
    constellation: "",
    distance: "",
    apparentMagnitude: "",
    diameter: "",
    discovered: "",
    dateCaptured: "",
    equipment: "Seestar S30",
    location: "Florida, USA",
    shortDescription: "",
    description: "",
    whatYouAreSeeing: "",
    whyItIsInteresting: "",
    captureNotes: "Captured with a Seestar S30."
  };
}

function corsHeaders() {
  return {
    "Content-Type": "application/json",
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Methods": "GET,POST,PUT,DELETE,OPTIONS",
    "Access-Control-Allow-Headers": "Content-Type,Authorization"
  };
}

function json(data, status = 200) {
  return new Response(JSON.stringify(data, null, 2), {
    status,
    headers: corsHeaders()
  });
}

function unauthorized() {
  return json({ ok: false, error: "Unauthorized" }, 401);
}

function checkAuth(request, env) {
  const header = request.headers.get("Authorization") || "";
  return header === `Bearer ${env.ADMIN_TOKEN}`;
}

function isImageKey(key) {
  return /\.(jpg|jpeg|png|webp)$/i.test(key);
}

function getExtension(key) {
  const match = key.match(/\.(jpg|jpeg|png|webp)$/i);
  return match ? match[0].toLowerCase() : ".jpg";
}

function getFolderFromKey(key) {
  return key.split("/").slice(0, -1).join("/");
}

function makePublicUrl(env, key) {
  return `${env.PUBLIC_R2_PUBLIC_BASE_URL.replace(/\/$/, "")}/${key}`;
}

function cleanFolder(folder) {
  return folder?.trim().replace(/\/$/, "");
}

async function readJsonObject(env, key, fallback = null) {
  const object = await env.ASTRO_PHOTO_BUCKET.get(key);
  if (!object) return fallback;

  try {
    return JSON.parse(await object.text());
  } catch {
    return fallback;
  }
}

async function putJson(env, key, data) {
  await env.ASTRO_PHOTO_BUCKET.put(key, JSON.stringify(data, null, 2), {
    httpMetadata: { contentType: "application/json" }
  });
}

async function getStatus(env) {
  return await readJsonObject(env, "_admin/status.json", {
    lastUploadAt: null,
    lastUploadKey: null,
    lastHideAt: null,
    lastHideKey: null,
    lastRestoreAt: null,
    lastRestoreKey: null,
    lastDeleteAt: null,
    lastDeleteKey: null,
    lastRedeployRequestedAt: null,
    lastRedeployStatus: null,
    lastInfoSaveAt: null,
    lastInfoFolder: null,
    lastMainImageAt: null,
    lastMainImageKey: null,
    lastObservationAt: null,
    lastObservationFolder: null
  });
}

async function updateStatus(env, patch) {
  const status = await getStatus(env);
  const updated = {
    ...status,
    ...patch,
    updatedAt: new Date().toISOString()
  };

  await putJson(env, "_admin/status.json", updated);
  return updated;
}

async function moveObject(env, fromKey, toKey) {
  const object = await env.ASTRO_PHOTO_BUCKET.get(fromKey);
  if (!object) return false;

  await env.ASTRO_PHOTO_BUCKET.put(toKey, object.body, {
    httpMetadata: object.httpMetadata
  });

  await env.ASTRO_PHOTO_BUCKET.delete(fromKey);
  return true;
}

async function getObservations(env, folder) {
  return await readJsonObject(env, `${folder}/observations.json`, []);
}

function summarizeObservations(observations) {
  const totalMinutes = observations.reduce(
    (sum, item) => sum + Number(item.minutes || 0),
    0
  );

  return {
    totalMinutes,
    totalHours: Math.round((totalMinutes / 60) * 10) / 10,
    sessions: observations.length
  };
}

export default {
  async fetch(request, env) {
    if (request.method === "OPTIONS") return json({ ok: true });

    if (!checkAuth(request, env)) return unauthorized();

    const url = new URL(request.url);

    if (url.pathname === "/status" && request.method === "GET") {
      return json({ ok: true, status: await getStatus(env) });
    }

    if (url.pathname === "/generate-info" && request.method === "GET") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      return json({
        ok: true,
        folder,
        info: getStarterInfoForFolder(folder)
      });
    }

    if (url.pathname === "/list" && request.method === "GET") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      const listed = await env.ASTRO_PHOTO_BUCKET.list({ prefix: `${folder}/` });

      const files = listed.objects
        .filter((object) => isImageKey(object.key))
        .filter((object) => !object.key.includes("/Raw/"))
        .map((object) => ({
          key: object.key,
          filename: object.key.split("/").pop(),
          size: object.size,
          uploaded: object.uploaded,
          url: makePublicUrl(env, object.key),
          isMain: /\/main\.(jpg|jpeg|png|webp)$/i.test(object.key)
        }))
        .sort((a, b) => {
          if (a.isMain && !b.isMain) return -1;
          if (!a.isMain && b.isMain) return 1;
          return a.filename.localeCompare(b.filename);
        });

      return json({ ok: true, folder, files });
    }

    if (url.pathname === "/hidden" && request.method === "GET") {
      const listed = await env.ASTRO_PHOTO_BUCKET.list({ prefix: "_hidden/" });

      const files = listed.objects
        .filter((object) => isImageKey(object.key))
        .map((object) => ({
          hiddenKey: object.key,
          originalKey: object.key.replace(/^_hidden\//, ""),
          filename: object.key.split("/").pop(),
          size: object.size,
          uploaded: object.uploaded,
          url: makePublicUrl(env, object.key)
        }))
        .sort((a, b) => a.hiddenKey.localeCompare(b.hiddenKey));

      return json({ ok: true, files });
    }

    if (url.pathname === "/upload" && request.method === "PUT") {
      const key = url.searchParams.get("key");

      if (!key) return json({ ok: false, error: "Missing key" }, 400);
      if (!isImageKey(key)) {
        return json({ ok: false, error: "Only jpg, jpeg, png, and webp are allowed" }, 400);
      }

      await env.ASTRO_PHOTO_BUCKET.put(key, request.body, {
        httpMetadata: {
          contentType: request.headers.get("Content-Type") || "application/octet-stream"
        }
      });

      const status = await updateStatus(env, {
        lastUploadAt: new Date().toISOString(),
        lastUploadKey: key
      });

      return json({
        ok: true,
        action: "uploaded",
        key,
        url: makePublicUrl(env, key),
        status
      });
    }

    if (url.pathname === "/make-main" && request.method === "POST") {
      const { key: sourceKey } = await request.json();

      if (!sourceKey) return json({ ok: false, error: "Missing key" }, 400);
      if (!isImageKey(sourceKey)) {
        return json({ ok: false, error: "Selected file is not an image" }, 400);
      }

      const sourceObject = await env.ASTRO_PHOTO_BUCKET.get(sourceKey);
      if (!sourceObject) {
        return json({ ok: false, error: "Source file not found", key: sourceKey }, 404);
      }

      const folder = getFolderFromKey(sourceKey);
      const extension = getExtension(sourceKey);
      const mainKey = `${folder}/main${extension}`;

      await env.ASTRO_PHOTO_BUCKET.put(mainKey, sourceObject.body, {
        httpMetadata: sourceObject.httpMetadata
      });

      const status = await updateStatus(env, {
        lastMainImageAt: new Date().toISOString(),
        lastMainImageKey: mainKey
      });

      return json({
        ok: true,
        action: "main_image_updated",
        sourceKey,
        mainKey,
        url: makePublicUrl(env, mainKey),
        status
      });
    }

    if (url.pathname === "/hide" && request.method === "POST") {
      const { key } = await request.json();

      if (!key) return json({ ok: false, error: "Missing key" }, 400);

      const hiddenKey = `_hidden/${key}`;
      const moved = await moveObject(env, key, hiddenKey);

      if (!moved) return json({ ok: false, error: "File not found", key }, 404);

      const status = await updateStatus(env, {
        lastHideAt: new Date().toISOString(),
        lastHideKey: key
      });

      return json({
        ok: true,
        action: "hidden",
        originalKey: key,
        hiddenKey,
        status
      });
    }

    if (url.pathname === "/restore" && request.method === "POST") {
      const { hiddenKey } = await request.json();

      if (!hiddenKey) return json({ ok: false, error: "Missing hiddenKey" }, 400);
      if (!hiddenKey.startsWith("_hidden/")) {
        return json({ ok: false, error: "Invalid hidden key" }, 400);
      }

      const originalKey = hiddenKey.replace(/^_hidden\//, "");
      const moved = await moveObject(env, hiddenKey, originalKey);

      if (!moved) {
        return json({ ok: false, error: "Hidden file not found", hiddenKey }, 404);
      }

      const status = await updateStatus(env, {
        lastRestoreAt: new Date().toISOString(),
        lastRestoreKey: originalKey
      });

      return json({
        ok: true,
        action: "restored",
        hiddenKey,
        originalKey,
        status
      });
    }

    if (url.pathname === "/delete-hidden" && request.method === "POST") {
      const { hiddenKey } = await request.json();

      if (!hiddenKey) return json({ ok: false, error: "Missing hiddenKey" }, 400);
      if (!hiddenKey.startsWith("_hidden/")) {
        return json({ ok: false, error: "Invalid hidden key" }, 400);
      }

      await env.ASTRO_PHOTO_BUCKET.delete(hiddenKey);

      const status = await updateStatus(env, {
        lastDeleteAt: new Date().toISOString(),
        lastDeleteKey: hiddenKey
      });

      return json({
        ok: true,
        action: "deleted_forever",
        hiddenKey,
        status
      });
    }

    if (url.pathname === "/info" && request.method === "GET") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      const info = await readJsonObject(env, `${folder}/info.json`, {
        name: folder.split("/").pop() || "",
        catalog: "",
        category: folder.startsWith("solar-system/") ? "Solar System" : "Deep Sky",
        objectType: "",
        constellation: "",
        distance: "",
        apparentMagnitude: "",
        diameter: "",
        discovered: "",
        shortDescription: "",
        description: "",
        dateCaptured: "",
        equipment: "Seestar S30",
        location: "Florida, USA",
        whatYouAreSeeing: "",
        whyItIsInteresting: "",
        captureNotes: ""
      });

      return json({ ok: true, folder, info });
    }

    if (url.pathname === "/info" && request.method === "PUT") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      const info = await request.json();
      await putJson(env, `${folder}/info.json`, info);

      const status = await updateStatus(env, {
        lastInfoSaveAt: new Date().toISOString(),
        lastInfoFolder: folder
      });

      return json({
        ok: true,
        action: "info_saved",
        folder,
        key: `${folder}/info.json`,
        info,
        status
      });
    }

    if (url.pathname === "/observations" && request.method === "GET") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      const observations = await getObservations(env, folder);

      return json({
        ok: true,
        folder,
        observations,
        summary: summarizeObservations(observations)
      });
    }

    if (url.pathname === "/observations" && request.method === "POST") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      const body = await request.json();
      const observations = await getObservations(env, folder);

      const observation = {
        id: crypto.randomUUID(),
        date: body.date || new Date().toISOString().slice(0, 10),
        minutes: Number(body.minutes || 0),
        subSeconds: Number(body.subSeconds || 0),
        notes: body.notes || "",
        createdAt: new Date().toISOString()
      };

      observations.push(observation);

      await putJson(env, `${folder}/observations.json`, observations);

      const status = await updateStatus(env, {
        lastObservationAt: new Date().toISOString(),
        lastObservationFolder: folder
      });

      return json({
        ok: true,
        action: "observation_added",
        folder,
        observation,
        observations,
        summary: summarizeObservations(observations),
        status
      });
    }

    if (url.pathname === "/delete-observation" && request.method === "POST") {
      const folder = cleanFolder(url.searchParams.get("folder"));
      if (!folder) return json({ ok: false, error: "Missing folder" }, 400);

      const body = await request.json();
      const observations = await getObservations(env, folder);
      const updated = observations.filter((item) => item.id !== body.id);

      await putJson(env, `${folder}/observations.json`, updated);

      return json({
        ok: true,
        action: "observation_deleted",
        folder,
        observations: updated,
        summary: summarizeObservations(updated)
      });
    }

    if (url.pathname === "/redeploy" && request.method === "POST") {
      if (!env.PAGES_DEPLOY_HOOK_URL) {
        return json({ ok: false, error: "Missing PAGES_DEPLOY_HOOK_URL" }, 500);
      }

      const response = await fetch(env.PAGES_DEPLOY_HOOK_URL, { method: "POST" });

      const status = await updateStatus(env, {
        lastRedeployRequestedAt: new Date().toISOString(),
        lastRedeployStatus: response.status
      });

      return json({
        ok: response.ok,
        action: "redeploy_requested",
        status: response.status,
        adminStatus: status
      });
    }

    return json({ ok: false, error: "Not found" }, 404);
  }
};